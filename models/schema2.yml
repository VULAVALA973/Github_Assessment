version: 2

models:
  - name: ORDERS1
    columns:
      - name: CUSTOMER_ID
        data_tests:
          - not_null
          - relationships:
              arguments:
                to: ref('CUSTOMERS1')
                field: CUSTOMER_ID