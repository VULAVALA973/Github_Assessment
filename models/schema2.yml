version: 2

models:
  - name: ORDERS1
    columns:
      - name: CUSTOMER_ID
        data_tests:
          - not_null
          - relationships:
              arguments:
                to: ref('CUSTOMERS')
                field: CUSTOMER_ID